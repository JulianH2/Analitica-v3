version: '3.8'

services:
  dashboard:
    build: .
    container_name: analytics_dashboard
    ports:
      - "8050:8050"
    volumes:
      - .:/app
    environment:
      - SECRET_KEY=tu_clave_secreta_aqui   
    restart: always